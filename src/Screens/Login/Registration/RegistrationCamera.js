import { View } from "react-native";
import React from "react";
import ShowCamera from "../../../component/ShowCamera";

const RegistrationCamera = () => {
  return (
    <View style={{ flex: 1 }}>
      <ShowCamera reg={"Registration3"} />
    </View>
  );
};

export default RegistrationCamera;
